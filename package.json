{
  "name": "internal-messaging-root",
  "private": true,
  "scripts": {
    "build": "cd server && npm install",
    "install-all": "cd docs && npm install && cd ../server && npm install",
    "start": "cd server && npm start"
  }
}
